11) Типы join'ов, поддерживаемых postgresql. Их отличия между собой. *

INNER JOIN, LEFT JOIN, RIGHT JOIN.
INNER JOIN - в результате получаем только общие записи обоих таблиц!
LEFT JOIN - в результате получаем все обьекты из левой таблицы, независимо от наличия соответствующих записей в правой таблице
RIGHT JOIN - в результате получаем все обьекты из правой таблицы, независимо от наличия соответствующих записей в правой таблице
